<Application x:Class="SimpleDataBindingApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SimpleDataBindingApp"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Logical Resources for Styling -->
        
        <!-- Font Sizes -->
        <System:Double x:Key="HeaderFontSize">24</System:Double>
        <System:Double x:Key="NormalFontSize">12</System:Double>
        <System:Double x:Key="LargeFontSize">16</System:Double>
        
        <!-- Colors and Brushes -->
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="Black"/>
        <SolidColorBrush x:Key="HighlightBrush" Color="Blue"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="Green"/>
        <SolidColorBrush x:Key="WarningBrush" Color="Red"/>
        <SolidColorBrush x:Key="HintBrush" Color="Gray"/>
        <SolidColorBrush x:Key="InfoBrush" Color="DarkSlateBlue"/>
        <SolidColorBrush x:Key="ProgressBrush" Color="DarkGreen"/>
        
        <!-- Thickness Resources -->
        <Thickness x:Key="StandardMargin">0,10,0,0</Thickness>
        <Thickness x:Key="SmallMargin">0,5,0,0</Thickness>
        <Thickness x:Key="LabelMargin">0,0,0,5</Thickness>
        <Thickness x:Key="HeaderMargin">0,0,0,20</Thickness>
        <Thickness x:Key="HorizontalMargin">10,0,0,0</Thickness>
        <Thickness x:Key="StandardPadding">5</Thickness>
        
        <!-- Styles -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource HeaderFontSize}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="{StaticResource HeaderMargin}"/>
        </Style>
        
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="{StaticResource LabelMargin}"/>
        </Style>
        
        <Style x:Key="HintTextStyle" TargetType="TextBlock">
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Foreground" Value="{StaticResource HintBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="{StaticResource HorizontalMargin}"/>
        </Style>
        
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="{StaticResource StandardPadding}"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        
    </Application.Resources>
</Application>
